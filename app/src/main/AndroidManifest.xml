<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabriel.practica3">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Splash"
            android:label="Valledupar Cesar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Hotels"
            android:label="@string/title_activity_hotels"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Bars" />
        <activity android:name=".Lugares" />
        <activity android:name=".Demography" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_Arawa"
            android:label="@string/title_activity_maps__arawa" />
        <activity
            android:name=".Maps_leyenda"
            android:label="@string/title_activity_maps_leyenda" />
        <activity
            android:name=".Maps_vajamar"
            android:label="@string/title_activity_maps_vajamar" />
        <activity
            android:name=".Maps_tierradecantores"
            android:label="@string/title_activity_maps_tierradecantores" />
        <activity
            android:name=".Mapskankarua"
            android:label="@string/title_activity_mapskankarua" />
        <activity
            android:name=".Mapsmamajuana"
            android:label="@string/title_activity_mapsmamajuana" />
        <activity
            android:name=".Mapsobeliso"
            android:label="@string/title_activity_mapsobeliso" />
        <activity
            android:name=".Mapsparque"
            android:label="@string/title_activity_mapsparque" />
        <activity
            android:name=".Mapssirenayrio"
            android:label="@string/title_activity_mapssirenayrio" />
        <activity
            android:name=".Mapsdemografia"
            android:label="@string/title_activity_mapsdemografia" />

    </application>

</manifest>
